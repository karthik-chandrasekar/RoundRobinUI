<h1 align="center"> Countries</h1>

<%= form_for :articles, method: "get", url: articles_add_country_path do |f| %>
    <p>
        <%= f.label :title %><br>
        <%= f.text_area :title %>
    </p>

    <p>
        <%= f.submit :value => 'Add Country' %>
    </p>

<% end %>

<%= link_to 'Back', new_article_path %>
<%= link_to 'Country List', articles_country_list_path %>
